meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm run build
  artifacts:
    "dist/*": /

run:
  command: "echo 'Static site ready'"
  containerPort: 80
