meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  skip: false
  additionalCommands: npm run build
  artifacts:
    "dist/**": /

run:
  command: npx serve -s . -l 80
  persistenceMount: /data
  containerPort: 80
  servicePort: 80
