meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  skip: false
  additionalCommands: npm run build
  artifacts:
    "dist/**": /

run:
  command: npx vite preview --host 0.0.0.0 --port 80
  persistenceMount: /data
  containerPort: 80
